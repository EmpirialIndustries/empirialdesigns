# Client-visible configuration only. Never put private API keys in VITE_* variables.
VITE_OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free

# Optional Firebase client configuration belongs in the app's public client config.
